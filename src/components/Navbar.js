import React from 'react'
import Searchbar from './Searchbar';

const Navbar = (props) => {

    return (
        <div id="navbarDiv">
            <Searchbar />
        </div>
    )

}

export default Navbar;